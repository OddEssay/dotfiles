#:qghostty +list-themes
# theme = dark:SoftServer,light:SoftServer
# theme = matrix
# theme = lovelace
theme = Teerb

# Fallback for systems without xterm-ghostty terminfo
# term = xterm-256color

window-decoration = false
confirm-close-surface = false
# tmux auto-start is handled in zshrc
